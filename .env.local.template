# Azure Functions API URL
# For local development, this should point to your local Azure Functions instance
# Example: http://localhost:7071
# For production, this should point to your deployed Azure Functions app
# Example: https://your-function-app.azurewebsites.net
AZURE_FUNCTIONS_URL=http://localhost:7071

# Azure Functions API key
# Required when calling functions that use function-level authorization (e.g., GetJobsHttpTrigger).
# Replace the placeholder value in your local .env file; do not commit real API keys to source control.
AZURE_FUNCTIONS_API_KEY=your-function-api-key-here

# Azure Blob Storage Configuration
# Storage account name for audio file uploads
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name

# Storage account key for generating SAS tokens
# Get this from Azure Portal -> Storage Account -> Access keys
AZURE_STORAGE_ACCOUNT_KEY=your-storage-account-key

# Container name for storing audio files (defaults to "audio-files" if not set)
AZURE_STORAGE_CONTAINER_NAME=audio-files
